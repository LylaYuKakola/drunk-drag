import * as React from 'react'
import { CellsStateType, CellType, ReducerActionType } from '../../../typings'

const { useState, useMemo, useCallback } = React

export default function useCommanderJump() {

}
